{
  "name": "@restorecommerce/identity-srv",
  "version": "0.2.8",
  "description": "Restorecommerce identity service",
  "main": "lib/start.js",
  "author": "n-fuse GmbH",
  "repository": {
    "type": "git",
    "url": "https://github.com/restorecommerce/identity-srv.git"
  },
  "license": "MIT",
  "keywords": [
    "restore",
    "commerce",
    "microservice",
    "identity",
    "srv"
  ],
  "dependencies": {
    "@restorecommerce/acs-client": "^0.4.14",
    "@restorecommerce/chassis-srv": "^0.3.2",
    "@restorecommerce/cluster-service": "^0.1.10",
    "@restorecommerce/grpc-client": "^0.2.10",
    "@restorecommerce/kafka-client": "^0.2.23",
    "@restorecommerce/logger": "^0.10.8",
    "@restorecommerce/protos": "^0.4.5",
    "@restorecommerce/resource-base-interface": "^0.2.0",
    "@restorecommerce/service-config": "^0.4.18",
    "bcryptjs": "^2.4.3",
    "cross-env": "^7.0.3",
    "lodash": "^4.17.21",
    "node-fetch": "^2.6.1",
    "source-map-support": "^0.5.20",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/mocha": "^9.0.0",
    "@types/node": "^16.9.1",
    "@typescript-eslint/eslint-plugin": "^4.31.1",
    "@typescript-eslint/eslint-plugin-tslint": "^4.31.1",
    "@typescript-eslint/parser": "^4.31.1",
    "coveralls": "^3.1.1",
    "eslint": "^7.32.0",
    "eslint-plugin-prefer-arrow-functions": "^3.0.1",
    "grpc-mock": "^0.7.0",
    "mocha": "^9.1.1",
    "nodemon": "^2.0.12",
    "npm-run-all": "^4.1.5",
    "nyc": "^15.0.1",
    "rimraf": "^3.0.0",
    "should": "^13.2.3",
    "ts-node": "^10.2.1",
    "tslint": "^6.1.3",
    "typescript": "^4.4.3"
  },
  "scripts": {
    "start": "node lib/start.js",
    "dev": "cross-env NODE_ENV=development nodemon --watch './src/**/*.ts' --exec 'ts-node' src/start.ts",
    "dev:nocluster": "cross-env NODE_ENV=development nodemon --watch './src/**/*.ts' --exec 'ts-node' src/worker.ts",
    "test": "npm run lint && nyc npm run mocha",
    "lint": "eslint src --ext .ts",
    "mocha": "cross-env NODE_ENV=test mocha --full-trace --exit --timeout 30000",
    "test-debug": "cpm run mocha -- --inspect-brk",
    "lcov-report": "nyc report --reporter=lcov",
    "coveralls": "nyc report --reporter=text-lcov | coveralls",
    "build:tsc": "tsc -d",
    "build:clean": "rimraf lib",
    "build": "npm-run-all lint build:clean build:tsc"
  },
  "engines": {
    "node": ">= 16.0.0"
  }
}
