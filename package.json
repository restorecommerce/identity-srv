{
  "name": "@restorecommerce/identity-srv",
  "version": "1.0.0",
  "description": "Restorecommerce identity service",
  "main": "lib/start.js",
  "author": "n-fuse GmbH",
  "repository": {
    "type": "git",
    "url": "https://github.com/restorecommerce/identity-srv.git"
  },
  "license": "MIT",
  "keywords": [
    "restore",
    "commerce",
    "microservice",
    "identity",
    "srv"
  ],
  "dependencies": {
    "@restorecommerce/acs-client": "^1.0.2",
    "@restorecommerce/chassis-srv": "^1.1.0",
    "@restorecommerce/cluster-service": "^1.0.0",
    "@restorecommerce/grpc-client": "^1.0.1",
    "@restorecommerce/kafka-client": "^1.0.2",
    "@restorecommerce/logger": "^1.1.0",
    "@restorecommerce/rc-grpc-clients": "^1.1.0",
    "@restorecommerce/resource-base-interface": "^1.1.0",
    "@restorecommerce/service-config": "^1.0.1",
    "bcryptjs": "^2.4.3",
    "cross-env": "^7.0.3",
    "jose": "^4.10.0",
    "lodash": "^4.17.21",
    "node-fetch": "^3.2.10",
    "oauth": "^0.10.0",
    "redis": "^4.3.1",
    "source-map-support": "^0.5.21",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@alenon/grpc-mock-server": "^3.0.12",
    "@restorecommerce/protos": "^1.1.0",
    "@types/mocha": "^10.0.0",
    "@types/node": "^18.8.4",
    "@types/oauth": "^0.9.1",
    "@typescript-eslint/eslint-plugin": "^5.39.0",
    "@typescript-eslint/eslint-plugin-tslint": "^5.39.0",
    "@typescript-eslint/parser": "^5.39.0",
    "coveralls": "^3.1.1",
    "eslint": "^8.24.0",
    "eslint-plugin-prefer-arrow-functions": "^3.1.4",
    "mocha": "^10.0.0",
    "nodemon": "^2.0.20",
    "npm-run-all": "^4.1.5",
    "nyc": "^15.1.0",
    "rimraf": "^3.0.2",
    "should": "^13.2.3",
    "ts-node": "^10.9.1",
    "tslint": "^6.1.3",
    "typescript": "^4.8.4"
  },
  "scripts": {
    "start": "node lib/start.js",
    "dev": "cross-env NODE_ENV=development nodemon --watch './src/**/*.ts' --exec 'ts-node' src/start.ts",
    "dev:nocluster": "cross-env NODE_ENV=development nodemon --watch './src/**/*.ts' --exec 'ts-node' src/worker.ts",
    "test": "npm run lint && nyc npm run mocha",
    "lint": "eslint src --ext .ts",
    "mocha": "cross-env NODE_ENV=test mocha --full-trace --exit --timeout 30000",
    "test-debug": "cpm run mocha -- --inspect-brk",
    "lcov-report": "nyc report --reporter=lcov",
    "coveralls": "nyc report --reporter=text-lcov | coveralls",
    "build:tsc": "tsc -d",
    "build:clean": "rimraf lib",
    "build": "npm-run-all lint build:clean build:tsc"
  },
  "engines": {
    "node": ">= 16.0.0"
  }
}
